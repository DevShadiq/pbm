# Admin Panel

Vue admin application for school operations, users, roles, permissions, institutions, master data, and student records.

Run the shared frontend development server from the repository root with `npm run dev`.

Local URL: `http://127.0.0.1:5173/admin/login`
